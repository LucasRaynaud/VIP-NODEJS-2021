<div class="listePhotos1">
      {{#each photos1}}
          <a href="/album/{{this.VIP_NUMERO}}"><img class="album" src="/images/vip/{{this.PHOTO_ADRESSE}}"></a>
      {{/each}}
</div>

<div class="photos">
    {{#if commentaire}}
        {{#each commentaire}}
            <div class="">
                <p class="souligne">{{this.VIP_PRENOM}} {{this.VIP_NOM}}</p>
                <img src="/images/vip/{{this.PHOTO_ADRESSE}}" alt="oups" class="imgAlbum">
                <fieldset>
                    <legend>Commentaire</legend>
                    <p>
                        {{#if this.PHOTO_COMMENTAIRE}}
                            {{this.PHOTO_COMMENTAIRE}}
                        {{else}}
                            <i>Cette photo ne possède pas de commentaire</i>
                        {{/if}}
                    </p>
                </fieldset>
            </div>
        {{/each}}
    {{else}}
        <p>
            Choisissez un vip pour voir l'album le concernant
        </p>
    {{/if}}
</div>
